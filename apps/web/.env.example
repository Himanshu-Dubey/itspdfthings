# ── API ───────────────────────────────────────────────────────────────────────
# Client-side: empty = relative URLs, proxied through Next.js rewrites (same-origin)
NEXT_PUBLIC_API_URL=
# Server-side: direct connection to API (for SSR/metadata)
API_URL=https://api.itspdfthings.com
